let num1: number = 4; //'inferences' the type of num1 as number even without explicitly mentioning a: number when a = 4

const num2 = 2; //'inferences' the type of num2 as 4 as the type or value cannot be changes to anything once a variable is declared as const
